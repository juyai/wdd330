//responsible for adding an event listener to the form to see when the user adds information. 
//When this happens, it will create a new instance of the model and then render the updated view.

import todoModel from '../js/todoModel';
import todoView from '../js/todoView';


// add event listeners for ALL TASKS|ACTIVE TASKS| COMPLETED TASKS --BOTTOMS
const controller = {
    
};
